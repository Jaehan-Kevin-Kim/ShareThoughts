import React from "react";
import PropTypes from "prop-types";

const PostImages = ({ images }) => {
  return <div>On the Contruction..</div>;
};

export default PostImages;

PostImages.propTypes = {
  images: PropTypes.arrayOf(PropTypes.object),
};
